version: '3'

services:
  apiserver:
    image: ghcr.io/taoist-labs/os-backend
    user: "1001:1000"
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./logs/:/superapp-backend/logs
      - ./conf/config.yml:/superapp-backend/conf/config.yml
